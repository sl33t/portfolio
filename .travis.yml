language: python
services:
  - "postgresql"
python:
  - "3.5"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
before_script:
  - "psql -c 'CREATE USER db_admin IDENTIFIED by '${DB_PASSWORD}';' -U postgres"
script:
  - "python manage.py test"